extends content-layout

block header
    a.btn.head-btn(href="/") Home

block content
    h2 Sign into system
    form#login-form.span4.round-bg(method="post")
        input.span4(type="text", name="login_name", required="required", placeholder="Login Name")
        input.span4(type="password", name="login_pass", required="required", placeholder="Password")
        button#sign-in.btn.btn-success(type="submit") Sign in
        a#res-pwd(href="#") Forget password?
